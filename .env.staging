# Frontend environment — staging (BUILD TIME ONLY)
# Baked into the JS bundle via: npm run build:staging
# NEXT_PUBLIC_* values are visible in the browser — do NOT put secrets here.

NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_WS_URL=ws://187.127.135.139:7080
