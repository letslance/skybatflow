# Frontend environment — production (BUILD TIME ONLY)
# Baked into the JS bundle via: npm run build:prod
# NEXT_PUBLIC_* values are visible in the browser — do NOT put secrets here.

NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_WS_URL=wss://yourdomain.com/ws
