# Frontend runtime environment — staging (RUNTIME ONLY)
# Loaded into the container via docker-compose env_file — NOT baked into the JS bundle.
# Safe to put server-side vars here (not visible in browser).

BACKEND_URL=http://api-gateway:7080
COOKIE_SECURE=false
